#include "ui_container.h"
#include <typeinfo>

UIContainer::UIContainer()
{
	//~ font_buffer = _font_buffer;
}
void UIContainer::draw(){
	//~ for (int i = 0; i < children.size(); i++)
	//~ {	
		//~ children[i]->draw();
	//~ }
	
}
//~ void UIContainer::addChild(UIBaseItem* child)
//~ {
	//~ child->bitmap_string.init(font_buffer);
	//~ children.push_back(child);
	//~ std::cout << "adding item of type <" << typeid(*child).name() << "> to UI CONTAINER\n";
//~ }


